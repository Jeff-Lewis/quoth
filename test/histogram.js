var quoth = require("../index");
    assert = require('assert');

describe('Histogram', function() {
    
    describe('#analyze(symbol, from, to, bars, (err, result) => { })', function () {
        it.skip('Returns an object', function () {
            
        });
    });
    
    describe('#histograp(quotes, fields, bars)', function () {
        it.skip('Returns an object', function () {
            
        });
    });
    
    describe('#print(name, stats)', function () {
        it.skip('Returns an object', function () {
            
        });
    });
    
});